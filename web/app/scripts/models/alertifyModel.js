/*global define*/
define([
    'backbone',
    'common',
    'logger'
], function(Backbone, Common, Logger) {
    'use strict';

    var AlertifyModel = Backbone.Model.extend({
        // Default attributes for the model

        initialize: function() {

        }

    });

    return AlertifyModel;
});